package main

// func (app *application) renderTemplates(w http.ResponseWriter, r *http.Request, templateName string) {
// 	ts, ok := app.templateCache[templateName]
// 	if !ok {
// 		http.Error(w, "Templates is not available", 500)
// 	}
// }
